name: job_market
version: '1.0.0'
config-version: 2
profile: job_market
model-paths: [models]
seed-paths: [seeds]
test-paths: [tests]
macro-paths: [macros]
analysis-paths: [analyses]
clean-targets: [target, dbt_packages]
models:
  job_market:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
